# Backend Framework and Server
# As specified in the tech stack for the server [cite: 24]
fastapi
uvicorn[standard]

# Frontend UI
# For creating the chatbot user interface [cite: 27]
streamlit

# RAG, LLM, and AI Components
# For the RAG pipeline, response generation with an LLM, and creating embeddings [cite: 21, 25]
langchain
langchain-openai  # Use this for GPT-3/4 models
sentence-transformers

# Vector Store Client
# For connecting to a vector database to store and retrieve document embeddings [cite: 26]
qdrant-client

# Utilities
# To load environment variables (like API keys) from a .env file
python-dotenv
# To handle PDF documents, which are common in FinTech for reports
pypdf
# To make HTTP requests from the Streamlit frontend to the FastAPI backend
requests
# RAG, LLM, and AI Components
langchain
langchain-core
langchain-community
langchain-openai
sentence-transformers
unstructured[md]
pip install -U langchain-huggingface
langchain_google_genai
google-generativeai
python-dotenv